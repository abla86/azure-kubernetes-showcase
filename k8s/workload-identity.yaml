apiVersion: v1
kind: ServiceAccount
metadata:
  name: api-workload-identity-sa
  namespace: showcase
  annotations:
    # Replace at deployment time with the Terraform output client ID.
    azure.workload.identity/client-id: "${AZURE_WORKLOAD_IDENTITY_CLIENT_ID}"
