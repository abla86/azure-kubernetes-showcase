apiVersion: apps/v1
kind: Deployment
metadata:
  name: care-portal-api
  namespace: showcase
  labels:
    app: care-portal-api
  annotations:
    checkov.io/skip1: "CKV_K8S_43=Internal image tag pinning used for demo"
    checkov.io/skip2: "CKV2_K8S_6=Default cluster network policy applied globally"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: care-portal-api
  template:
    metadata:
      labels:
        app: care-portal-api
        azure.workload.identity/use: "true"
    spec:
      automountServiceAccountToken: false
      serviceAccountName: api-workload-identity-sa
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: api
          image: acrshowcasedev.azurecr.io/care-portal-api:${IMAGE_TAG}
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
          securityContext:
            runAsNonRoot: true
            runAsUser: 10001
            runAsGroup: 10001
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          resources:
            requests:
              cpu: 100m
              memory: 64Mi
            limits:
              cpu: 300m
              memory: 256Mi
          startupProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 2
            failureThreshold: 30
          readinessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: care-portal-service
  namespace: showcase
spec:
  selector:
    app: care-portal-api
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
